# /pricing 与 teamorouter.com 的框架差距（2026-09-02 比对）

崔涣的要求是「页面风格保留，**框架上参考 teamorouter.com**」。风格保留和充值区块已交付，
这份记录的是**框架层面还差什么、为什么没做、什么时候做才诚实**。

## 逐块比对

| teamorouter.com 的区块 | 我们有没有 | 为什么 |
|---|---|---|
| 导航 / hero / 页脚 | ✅ 有 | — |
| 充值换算（$10–$200 档 + 约等于多少 token） | ✅ **已交付** | 就是崔涣给的那张参考图 |
| 「一个 credit 价，所有模型」费率说明 | ✅ 有（Rate card 段） | — |
| **实时模型价格表**（标价 vs 折后，可搜可筛，带上下文长度/缓存率/供给方） | ❌ **没有** | 见下 §A |
| **信任背书**（"Trusted by 6,000+ dev teams" + 伙伴 logo） | ❌ 没有 | 见下 §B |
| **关键指标**（"139B+ tokens routed yesterday"、">99% cache hit"、"99.98% routing SLA"） | ❌ 没有 | 见下 §B |
| **用量榜**（日 token 量、热门 agent / 模型排行） | ❌ 没有 | 见下 §B |
| 快速接入代码示例 | ◐ 在 `/docs/router`，不在 `/pricing` | 见下 §C |

## §A 模型价格表：不是不做，是现在做就是假的

两个各自独立、都足以否决的理由：

1. **`default` 分组现在一条渠道都没有**（见 `Runix-lab/runix-gateway` 的
   `config/GO-LIVE-CHECKLIST.md` §1）。列一张"我们支持这些模型、这个价"的表，
   而自助客户一个都调不通 —— 那是虚假宣传，不是营销。
2. **列价必然点名供给方**（OpenAI/Anthropic/Google）。而 `docs/SUPPLY-COMPLIANCE.md` §3
   专门核过并记下：**全站没有点名任何上游供给方，这个克制在合规上正在起保护作用**
   （转售授权尚未取得的前提下）。一张模型价格表会一次性把它破坏掉。

**结论：这张表要等第 1 条供给落地之后再做，并且要从网关自己的 `ModelRatio` 生成，
不能手写**（手写必然与线上分叉，`config/apply-config.sh` 已经有过分叉的教训）。

## §B 指标与背书：charter 禁止

`docs/CHARTER.md`「对外真实」：**不编造客户/数据；指标只用 target 口径；示意数据必标"示意"**。

teamorouter 那几块之所以成立，是因为它**真有流量**。我们自助线现在
**零客户、零流量**（`logs` 表里 `default` 分组的成功请求停在 2026-08-20 那一条）。
把"6,000+ 团队""昨日 139B tokens""99.98% SLA"照搬过来就是编数字 —— 这条不用讨论。

**能诚实写的只有 target 口径的 SLA**，而它已经在 `/security` 与 `/router` 上了。

## §C 代码示例：可以做，但要先想清楚

把 `/docs/router` 的三行接入示例搬到 `/pricing`，技术上安全、内容上诚实。
**但现在搬过去会给人"填个 base URL 就能跑"的暗示，而自助路径实际跑不通。**

→ 建议与 §A 一起做：**供给落地当天**，模型表 + 代码示例 + CTA 改文案，一次性上。
在那之前，页面当前措辞（CTA=「Request access」、
「self-serve card checkout is being switched on」）是**准确**的，不要提前改成「立即购买」。

## 一句话

**teamorouter 的框架是「有供给 + 有流量」长出来的。** 我们把它的**结构**学到了
（充值前置、费率透明、逐产品定价），学不了的是**需要真实数据才成立的那几块**。
差距不在页面工程，在第 1 条供给 —— 供给一落地，这份清单就是当天的施工单。
