{
  "name": "runix-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Runix marketing site (static HTML/CSS) plus a reserved, provider-agnostic commerce layer (types + money-safety utilities). No runtime backend; no build step for the site.",
  "scripts": {
    "test": "node --test payments/*.test.js"
  }
}
